# Ada instance name (without .ada.support)
# e.g. grab-my-gen or roystonlee-csc.us2
ADA_INSTANCE_NAME=

# Ada API key (found in Settings → Integrations → API)
ADA_API_KEY=

# Default Grab language locale pre-filled in the UI
# e.g. en-my, en-ph, en-sg
GRAB_LANGUAGE_LOCALE=en-ph
